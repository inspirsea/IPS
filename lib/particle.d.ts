export declare class Particle {
    lifeTime: number;
    startTime: number;
    startPosition: [number, number];
    velocity: [number, number];
    constructor(lifeTime: number, startTime: number, startPosition: [number, number], velocity: [number, number]);
}
